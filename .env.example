# Optional: connect LLeveLL Signal to a real AI backend.
# The UI works immediately without this variable using its built-in site-aware guide.
# Your endpoint should accept POST JSON: { message, history, context }
# and return at minimum: { "reply": "..." }
# Optional actions: [{ "label": "View projects", "type": "navigate", "target": "team" }]
VITE_LLEVELL_CHAT_ENDPOINT=/api/chat
